# Pipeline I/O — large and fully reproducible from corpus/raw + scripts.
# Only context_layer/ is committed.
corpus/

# Model self-use working notes. Removed at the end of each dev cycle.
.agent/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.env.local

# minerU local model / cache
.mineru_cache/
models/

# Claude Code per-user permission cache.
# settings.local.json can capture full command lines, including any inline
# secrets (API tokens etc.) — keep it out of git.
.claude/settings.local.json

context_layer/
